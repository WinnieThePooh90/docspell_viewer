package paulokat.de.docspellviewer.ui

import androidx.compose.ui.graphics.Color

/** Feste Farben für die Filter-Sidebar (unabhängig vom Farbschema). */
object FilterSidebarColors {
    val background = Color.White
    val text = Color(0xFF212121)
    val textMuted = Color(0xFF757575)
    val icon = Color(0xFF212121)
    val divider = Color(0xFFE0E0E0)
    val listBackground = Color(0xFFFAFAFA)
}
